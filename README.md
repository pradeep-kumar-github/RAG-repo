# RAG-repo
Retrieval-Augmented Generation (RAG) is an AI framework that improves Large Language Model (LLM) outputs by fetching relevant data from external, trusted knowledge bases before generating a response. It reduces hallucinations and ensures up-to-date, specialized answers without retraining the model. 
